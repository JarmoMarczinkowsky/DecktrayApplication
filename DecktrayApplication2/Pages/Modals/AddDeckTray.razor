<div class="myModal">

    <div class="myForm">
        <div class="form-group">
            <label for="amountOfTrays">Amount of trays:</label>
            <input type="number" id="amountOfTrays" value="1" min="1" max="10" @onchange="numberChange" />
        </div>
    </div>

    <div class="previewDecktray testing">
        <div class="outsideDeckTray" id="previewDecktrayOutline">
            <div class="lhDeckTray" id="previewDecktray">
                @for (int i = 0; i < inputNumber; i++)
                {
                    float calcAmountOfRacks = 100 / inputNumber;
                    <div id="previewDecktray_test" style="width:100%;height:@calcAmountOfRacks%;border:1px solid lightgreen;"></div>
                }
            </div>
            <div class="trapezoid"></div>
        </div>
    </div>
</div>
<button>Ok</button>
<button>Cancel</button>

<style>
    .myModal {
        display: flex;
        justify-content: space-between;
    }

    .myForm {
        width: 250px;
    }

    .form-group {
        display: flex;
        justify-content: space-between;
    }

    .previewDecktray {
        margin-left: 10px;
        width: 250px;
        height: 250px;
        background: lightgray;
        display: flex;
        align-items: center;
        justify-content: center;
    }
</style>

@code {
    const float lengthOfDecktray = 175;
    float lengthOfRack;

    short inputNumber = 1;
    float test;



    private void numberChange(ChangeEventArgs e)
    {
        string myNumber = e.Value.ToString();
        inputNumber = short.Parse(myNumber);

        if (inputNumber < 1)
        {
            inputNumber = 1;
        }
        else if (inputNumber > 10)
        {
            inputNumber = 10;
        }

        Console.WriteLine($"racks: {inputNumber}");
    }

    public string RackSize()
    {
        lengthOfRack = 100 / inputNumber;

        Console.WriteLine(lengthOfRack);
        return lengthOfRack.ToString() + "%";
    }
}